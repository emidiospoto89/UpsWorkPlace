UpsWorkPlace - starter project

Files:
- App.js: first port of the working APK logic into an Expo/WebView shell.
- package.json: Expo + React Native dependencies.
- app.json: Android/Expo project configuration.
- eas.json: Android APK build configuration.

Important:
- The original Ruolino PDF already exists in the GitHub repository.
- The official logo image is already in the repository. Rename the logo image to icon.png
  before the first build, or tell me which of the two PNG files is the logo and I will
  adjust app.json accordingly.
- This is intentionally the first port of the existing working APK. The new side menu
  and the exact original Ruolino PDF generation are the next development step.
